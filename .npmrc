# Flat node_modules so Next.js standalone file-tracing and the Dockerfile's
# Prisma engine copies resolve real paths (pnpm's default symlinked layout
# can leave them dangling in the standalone output).
node-linker=hoisted
