# Build the two Dockerfile targets and push them to GitHub Container Registry
# (Phase 10, spec §4 Reference C). The cluster pulls a tagged image from
# ghcr.io — no in-cluster builds.
#
# Tags published per ref:
#   merge to main → ghcr.io/<owner>/herd-scheduler:latest + :sha-<shortsha>
#   tag vX.Y.Z    → :X.Y.Z (+ :latest)
# and the migration one-shot as the same tags with a `-migrate` suffix
# (e.g. :latest-migrate), built from the `migrate` target.

name: Publish image

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  workflow_dispatch:

concurrency:
  group: publish-image-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/herd-scheduler

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: App image metadata
        id: meta-app
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=sha,prefix=sha-

      - name: Migrate image metadata
        id: meta-migrate
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=sha,prefix=sha-
          flavor: |
            suffix=-migrate

      - name: Build and push app image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: runner
          push: true
          tags: ${{ steps.meta-app.outputs.tags }}
          labels: ${{ steps.meta-app.outputs.labels }}
          cache-from: type=gha
          # mode=min (not max): the `builder` stage is invalidated by `COPY . .`
          # on every commit, so exporting its (large) layers each run is ~2 min of
          # wasted upload for a near-zero hit rate. The reusable `deps` layer is
          # kept cached by the migrate build's mode=max below.
          cache-to: type=gha,mode=min

      - name: Build and push migrate image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: migrate
          push: true
          tags: ${{ steps.meta-migrate.outputs.tags }}
          labels: ${{ steps.meta-migrate.outputs.labels }}
          cache-from: type=gha
          # mode=max here is cheap — the migrate target only references the `deps`
          # stage (no `builder`/.next output), so this exports the install layer
          # both images reuse, keeping `pnpm install` cached across runs.
          cache-to: type=gha,mode=max
